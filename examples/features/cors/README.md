# @banhmi/security — CorsModule example

Single allowed origin CORS demo. Shows how `CorsModule.forRoot()` handles
preflight requests and reflects the correct `Access-Control-Allow-Origin`
header for allowed origins.

## Run

```bash
bun test examples/features/cors
```
