# @banhmi/security — HelmetModule example

Minimal app demonstrating how `HelmetModule.forRoot()` appends OWASP-recommended
security headers to every response.

## Run

```bash
bun test examples/features/helmet
```
