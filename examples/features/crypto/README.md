# @banhmi/crypto — feature example

Demonstrates password hashing (Argon2id via `Bun.password`), symmetric
encryption (AES-256-GCM via Web Crypto), and secure random token generation.

## Run

```bash
bun test examples/features/crypto
```
